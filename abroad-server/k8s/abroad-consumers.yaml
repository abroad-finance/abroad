apiVersion: apps/v1
kind: Deployment
metadata:
  name: abroad-consumers
  labels:
    app: abroad-consumers
spec:
  replicas: 1
  selector:
    matchLabels:
      app: abroad-consumers
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: abroad-consumers
    spec:
      serviceAccountName: abroad-stellar-sa
      containers:
        - name: abroad-consumers-container
          image: abroad-consumers-container
          command: ["node"]
          args: ["dist/app/workers/consumers/server.js"]
          imagePullPolicy: Always
          env:
            - name: HEALTH_PORT
              value: "3000"
            - name: SENTRY_DSN
              valueFrom:
                secretKeyRef:
                  name: sentry-dsn
                  key: SENTRY_DSN
            - name: SENTRY_ENVIRONMENT
              value: "${_SENTRY_ENVIRONMENT}"
            - name: SENTRY_RELEASE
              value: "${COMMIT_SHA}"
          resources:
            requests:
              cpu: "1"
              memory: "1Gi"
            limits:
              cpu: "2"
              memory: "2Gi"
          ports:
            - name: http
              containerPort: 3000
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
