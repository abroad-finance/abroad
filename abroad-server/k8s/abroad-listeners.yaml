apiVersion: apps/v1
kind: Deployment
metadata:
  name: abroad-listeners
  labels:
    app: abroad-listeners
spec:
  replicas: 1
  selector:
    matchLabels:
      app: abroad-listeners
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: abroad-listeners
    spec:
      serviceAccountName: abroad-stellar-sa
      containers:
        - name: abroad-listeners-container
          image: abroad-listeners-container
          command: ["node"]
          args: ["dist/app/workers/listeners/server.js"]
          imagePullPolicy: Always
          env:
            - name: HEALTH_PORT
              value: "3000"
            - name: SENTRY_DSN
              valueFrom:
                secretKeyRef:
                  name: sentry-dsn
                  key: SENTRY_DSN
            - name: SENTRY_ENVIRONMENT
              value: "${_SENTRY_ENVIRONMENT}"
            - name: SENTRY_RELEASE
              value: "${COMMIT_SHA}"
          resources:
            # Right-sized to measured 7-day usage (peak 82m CPU / 178Mi). Autopilot
            # bills on requests; cluster supports bursting so limits stay high for burst.
            requests:
              cpu: "100m"
              memory: "320Mi"
            limits:
              cpu: "2"
              memory: "2Gi"
          ports:
            - name: http
              containerPort: 3000
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
